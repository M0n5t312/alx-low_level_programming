#include <stdio.h>
#include <stdlib.h>
/**
 * main - adds positive numbers
 * @argc: count
 * @argv: array
 * Return: 1 Success
 */
int main(int argc, char *argv[])
{
	int i, num;

	if (argc == 0)
		printf("%d\n", argc);
	else
		for (i = 1; i < argc; i++)
		{
			if (*(argv + i) != 'a' || *(argv + i) != 'z'
		}
}
