#include "holberton.h"
#include <stdio.h>
/**
 * main - prints name of program
 * @argc: count
 * @argv: array
 * Return: 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
